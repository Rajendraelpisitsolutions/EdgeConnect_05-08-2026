<!--
  Elpis EdgeConnect — License Generator (admin desktop tool).
  A WinForms form that produces a signed license.json for gateway activation.
  Reuses ElpisEdgeConnect.Core's CanonicalJson + LicenseSignatureValidator so the
  generated file is byte-for-byte compatible with the runtime's verifier.

  NOT part of ElpisEdgeConnect.sln (Windows-only). Published as a standalone,
  self-contained win-x64 desktop app. See docs for the publish command.
-->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ElpisEdgeConnect.LicenseGeneratorApp</RootNamespace>
    <AssemblyName>ElpisLicenseGenerator</AssemblyName>
    <!-- Elpis logo as the app/window/taskbar icon (same icon as the installer
         and the EdgeConnect exes). Path is relative to this csproj. -->
    <ApplicationIcon>..\..\installer\elpis.ico</ApplicationIcon>
    <!-- Standalone tool — do not inherit the repo's strict analyzer gate. -->
    <TreatWarningsAsErrors>false</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>false</EnforceCodeStyleInBuild>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <AnalysisLevel>none</AnalysisLevel>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.0.10" />
  </ItemGroup>

  <!-- Elpis wordmark shown in the locked header bar; embedded so the standalone
       exe needs no loose asset file at runtime. Loaded via GetManifestResourceStream. -->
  <ItemGroup>
    <EmbeddedResource Include="..\..\installer\elpis-logo.png" LogicalName="ElpisLogo.png" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\ElpisEdgeConnect.Core\ElpisEdgeConnect.Core.csproj" />
  </ItemGroup>

</Project>
